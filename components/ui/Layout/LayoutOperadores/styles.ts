import { StyleSheet } from "react-native";

export default StyleSheet.create({
    operators: {
        flexDirection: 'row',
        justifyContent: 'space-between',
        marginVertical: 10,
      }
})